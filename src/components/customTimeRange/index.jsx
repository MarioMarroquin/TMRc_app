import TimeRangePicker from '@wojtekmaj/react-timerange-picker';
import './index.css';

const CustomTimeRange = (props) => {
	return <TimeRangePicker {...props} />;
};

export default CustomTimeRange;
